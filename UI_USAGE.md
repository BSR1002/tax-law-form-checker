# 다중 법령·버전 비교 UI

## 실행

프로젝트 루트의 .env 또는 실행 환경에 LAW_OC를 설정합니다. 환경변수가 있으면 .env보다 우선합니다. 키는 화면에 입력하지 않습니다.

~~~bash
python -m pip install -r requirements.txt
python -m streamlit run streamlit_app.py --server.address 0.0.0.0 --server.port 8501
~~~

Codespaces의 포트 탭에서 8501을 열면 됩니다. Codespace와 Streamlit이 실행 중이어야 주소가 열립니다.

## 사용 흐름

1. 사이드바에서 '법령·버전 선택 후 실행'을 선택합니다.
2. '법인세법 시행규칙', '조세특례제한법 시행규칙' 등 정확한 법령명을 입력합니다.
3. '현행·연혁 조회'를 누릅니다. 기존 LawClient.histories(name)의 nw="1,3" 조회와 페이지 처리를 재사용합니다.
4. 시행일과 MST를 보고 현재 버전과 기준 버전을 선택합니다. 첫 항목은 최신 시행일의 버전이며, 같은 시행일도 MST로 구분합니다.
5. 'MANUAL Scanner 실행'을 누릅니다. 같은 버전 또는 역전된 시행일은 실행할 수 없습니다.
6. 기존 변경/신규/삭제/확인 필요 탭에서 결과를 봅니다. 변경 탭의 검색 및 상세 비교 표도 동일합니다.

'저장된 결과 (읽기 전용)'은 기존 scan_results.json을 읽습니다. 이 파일이 없어도 수동 실행은 가능합니다.

## 결과와 인증

- UI는 기존 scanner.scan(..., mode="MANUAL")의 반환값을 사용합니다. 결과/상태 JSON 파일을 생성하거나 수정하지 않습니다.
- 결과와 연혁 목록은 Streamlit 세션별로 보관합니다. 법령명/버전 변경 또는 재조회/재실행 시 이전 결과를 비워 혼동을 방지합니다. 새로고침이나 세션 종료 시 결과가 사라질 수 있습니다.
- LAW_OC는 .env/환경변수에서만 읽으며 입력 위젯, 세션 상태, 전역 캐시에 저장하지 않습니다. HTTP 오류 본문/URL/예외 원문은 UI에서 출력하지 않습니다.
- 일부 서식이 실패하면 기존 Scanner의 partial 결과와 확인 필요 탭을 표시합니다.
- Comparator/Scanner의 비교 로직, CLI의 AUTO 상태 관리, 기존 테스트는 변경하지 않습니다.

## 테스트

~~~bash
python -m unittest discover -v
~~~

추가 테스트는 실제 Streamlit AppTest와 모의 API를 사용합니다. 다중 법령 전달, 현행+연혁 조회, 버전 검증, 표/탭 렌더링, 검색, 세션 분리, 파일 보존, 부분 실패 및 인증값 비노출을 검증합니다.

## 작업 검증 결과 (2026-09-18)

- 기존 24개 + 추가 18개, 총 42개 unittest 통과.
- 실제 API 연혁 조회: 법인세법 시행규칙 169개, 조세특례제한법 시행규칙 114개.
- 브라우저에서 조세특례제한법 시행규칙 2026-07-01(MST 284611) → 2026-09-18(MST 289267) MANUAL 실행 완료.
- 총 394개 서식: 변경 1개, 신규 0개, 삭제 0개, 확인 필요 0개. 변경 서식의 상세 비교 표 7건 표시 확인.
- 기존 Scanner/Comparator/테스트/결과·상태 JSON 및 기존 사용자 수정 파일의 해시 보존 확인.

